// Страница сброса пароля. Использует фичу ResetPasswordForm.
import { ResetPasswordForm } from '@/features/auth/reset-password';

const ResetPasswordPage = () => {
  return <ResetPasswordForm />;
};

export default ResetPasswordPage;